{
  "$schema": "https://json.schemastore.org/mcp-config.json",
  "mcpServers": {
    "chrome-devtools": {
      "command": "npx",
      "args": [
        "-y",
        "chrome-devtools-mcp@latest",
        "--auto-connect"
      ],
      "env": {
        "CHROME_DEVTOOLS_MCP_NO_USAGE_STATISTICS": "true"
      },
      "description": "Chrome DevTools MCP Server for dynamic browser automation, live QA error audits, UI/UX color contrast verification, and user manual screenshot generation."
    }
  }
}
